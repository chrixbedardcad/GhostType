/**
 * Update popup — placeholder for Phase 3 migration.
 */
export function UpdateWindow() {
  return (
    <div className="h-full flex items-center justify-center bg-base p-6">
      <p className="text-subtext-0 text-sm">Update — React migration in progress</p>
    </div>
  );
}
